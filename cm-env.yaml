apiVersion: v1
kind: Pod
metadata:
  name: env-pod
spec:
  containers:
    - name: env-pod
      command: ["/bin/sh", "-c", "env && sleep 3600"]
      image: busybox:latest
      envFrom:
        - configMapRef:
            name: app-config
  restartPolicy: Never
